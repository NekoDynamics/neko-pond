# Interaction

## 中文

交互模块负责触摸信号聚合、低延迟响应、输入过滤和反馈调度。该层连接用户输入与生态状态，但不得直接耦合模型推理。

## English

The interaction module owns touch aggregation, low-latency response, input filtering, and feedback scheduling. It connects input to ecosystem state without directly coupling model inference.
